/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'images.unsplash.com',
      't3.ftcdn.net',
      'lh3.googleusercontent.com',
    ],
  },
}

module.exports = nextConfig
